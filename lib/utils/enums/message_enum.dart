enum MessageType {
  unknown,
  text,
  image,
  video,
  multipleMedia,
  notice,
  voice,
  location,
}

enum MessageStatus { unknown, sending, sent, error }

enum MessagePosition { unknown, top, midle, bottom }
